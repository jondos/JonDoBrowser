diff -r 28af55043104 browser/app/Makefile.in
--- a/browser/app/Makefile.in	Fri Aug 24 12:03:18 2012 -0700
+++ b/browser/app/Makefile.in	Fri Aug 31 08:37:20 2012 +0200
@@ -112,16 +112,19 @@ ifndef GNU_CC #{
 LDFLAGS += /HEAP:0x40000
 ifeq ($(OS_TEST),x86_64) #{
 # set stack to 2MB on x64 build.  See bug 582910
 LDFLAGS += -STACK:2097152
 endif #}
 endif #}
 endif #}
 
+CXXFLGS += -fPIE
+LDFLAGS += -pie
+
 ifneq (,$(filter-out OS2 WINNT,$(OS_ARCH)))
 
 libs:: 
 	cp -p $(MOZ_APP_NAME)$(BIN_SUFFIX) $(DIST)/bin/$(MOZ_APP_NAME)-bin$(BIN_SUFFIX)
 
 GARBAGE += $(addprefix $(DIST)/bin/defaults/pref/, firefox.js)
 
 endif
