diff -r 2c818c135442 browser/app/Makefile.in
--- a/browser/app/Makefile.in	Mon Aug 06 16:04:30 2012 -0700
+++ b/browser/app/Makefile.in	Mon Aug 20 17:35:55 2012 +0200
@@ -145,16 +145,19 @@ ifndef GNU_CC #{
 LDFLAGS += /HEAP:0x40000
 ifeq ($(OS_TEST),x86_64) #{
 # set stack to 2MB on x64 build.  See bug 582910
 LDFLAGS += -STACK:2097152
 endif #}
 endif #}
 endif #}
 
+CXXFLAGS += -fPIE
+LDFLAGS += -pie
+
 ifneq (,$(filter-out OS2 WINNT,$(OS_ARCH)))
 
 libs:: 
 	cp -p $(MOZ_APP_NAME)$(BIN_SUFFIX) $(DIST)/bin/$(MOZ_APP_NAME)-bin$(BIN_SUFFIX)
 
 GARBAGE += $(addprefix $(DIST)/bin/defaults/pref/, firefox.js)
 
 endif
